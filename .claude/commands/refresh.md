Refresh the show listings by running the regenerate_shows.py script.

1. Run `python regenerate_shows.py` in the project directory
2. Report how many shows were generated and confirm the refresh was successful
3. Open the shows.html page in Chrome so I can see the updated listings
